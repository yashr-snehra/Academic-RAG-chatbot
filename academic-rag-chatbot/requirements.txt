# Pinned dependencies for pip users (Poetry users: see pyproject.toml).
# Install:  pip install -r requirements.txt
# Tested on Python 3.11–3.13.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.137.2
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# ── RAG stack (LangChain 0.3 line + local Ollama) ─────────────────────────────
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.31
langchain-ollama==0.2.3
langchain-qdrant==0.2.1
langchain-text-splitters==0.3.11

# ── Vector store, cache, PDF, config ──────────────────────────────────────────
qdrant-client==1.18.0
redis[hiredis]==8.0.0
pymupdf==1.27.2.3
pydantic==2.13.4
pydantic-settings==2.14.1
httpx==0.28.1

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest==9.1.0
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# ── Optional extras (not required to run the app) ─────────────────────────────
# Quality evaluation:  pip install ragas pandas
# Load testing:        pip install locust
# Hybrid dense+BM25 search (HYBRID_ENABLED=true, then recreate collection + re-ingest):
#                      pip install fastembed
# Cross-encoder reranking (RERANK_ENABLED=true; first run downloads ~80MB model):
#                      pip install sentence-transformers
